kind: Service
apiVersion: v1
metadata:
  name: altinity-dashboard
  namespace: clickhouse
spec:
  type: ClusterIP
  selector:
    application: altinity-dashboard
    role: altinity-dashboard
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
